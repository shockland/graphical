name: Graphical
goal: |
  Improve the Graphical macOS app (Sources/GraphicalApp and related modules).
  Agents should make real source changes, not just write markdown in artifacts.
  Keep changes focused and minimal; swift build must pass before each handoff completes.
goalFile: GOAL.md
defaultMaxIterations: 3
defaultTimeoutSeconds: 600
