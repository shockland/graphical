runners:
  cursor_agent:
    command: /bin/bash
    args:
    - -lc
    - 'set -euo pipefail

      export PATH="$HOME/.local/bin:$PATH"

      cursor-agent -p --output-format stream-json --stream-partial-output --trust --force --workspace {{project_root}} --model {{model}}
      "$(cat {{prompt_file}})"

      '
    cwd: '{{project_root}}'
    env: {}
    kind: cursor_agent
    default_model: composer-2.5-fast
