# Goal

Improve the Graphical macOS app (`Sources/GraphicalApp` and related modules).

Agents should make **real source changes**, not just write markdown in `.graphical/artifacts/`.

Keep changes focused and minimal. `swift build` must pass before implementer and reviewer handoffs complete.

Enter a specific task below, then press **Play** (⌘R) to run the org.
